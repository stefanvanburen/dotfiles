# NOTE: bat _must_ have less installed via homebrew to function

# GitHub looks better on light themes
--theme="GitHub"
# Nord looks better on dark themes
# --theme="Nord"

# Show line numbers, Git modifications and file header (but no grid)
--style="numbers,changes,header"

# Add mouse scrolling support in less (does not work with older
# versions of "less")
# This means that less needs to be installed via homebrew, or this won't work.
--pager="less -FR"

# Use "gitignore" highlighting for ".ignore" files
--map-syntax .ignore:.gitignore
