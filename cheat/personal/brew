# Generate a Brewfile with `brew bundle`
brew bundle dump

# Install contents of a Brewfile
brew bundle

# Remove dependencies of only uninstalled brews
brew bundle dump && brew bundle --force cleanup
