# globally ignore direnv files
.direnv
.envrc

# ignore my personal .ignore files
.ignore
.fdignore
.rgignore

# python writes compiled bytecode to .pyc files in __pycache__ directories
__pycache__

# ignore my own pre-commit hooks
.pre-commit-config.yaml

# macOS
*.DS_Store

# clojure
# clojure-lsp generated files
.lsp
# https://github.com/clj-kondo/clj-kondo cache files
.clj-kondo
# classpath cache
# https://clojure.org/reference/deps_and_cli
# > The next several steps of this execution can be skipped if we have already
# > computed the classpath and cached it. Classpath and the runtime basis files
# > are cached in the current directory under .cpcache/.
.cpcache
# created by nrepl
.nrepl-port

# Project-local neovim configuration.
# Technically, .nvim.lua can be shared with other neovim users that also enable
# `.exrc`, but there are so many other ways to share configuration
# (.editorconfig, etc.).
# Ref: https://stefan.vanburen.xyz/til/nvim-nfnl-directory-configuration/
.nfnl.fnl
.nvim.fnl
.nvim.lua
flsproject.fnl

# Claude
**/.claude/*
# https://code.claude.com/docs/en/memory
CLAUDE.local.md

# https://github.com/charmbracelet/crush
.crush

# https://github.com/badlogic/pi-mono/tree/main/packages/coding-agent
.pi

# kotlin-language-server
kls_database.db

# JetBrains IDEs
.idea
